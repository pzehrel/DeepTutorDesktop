{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the DeepTutorDesktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
